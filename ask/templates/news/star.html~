{% extends "news/mdlbase.html" %}

{% block title %}{% endblock %}

{% block content %}
<div class="row">&nbsp;</div>
	<div class="container" >
	<h1>{{star.name  }} </h1>
	<img src="{{ star.photo }}" class="img-rounded" width="400" height="350"></img>
        <br/>
        {% for news in newses %}
    	<p> {{ news.text }}</p>
	<a href="{{ news.url }}">{{ news.header }} </a>
        {% endfor %}
</div>
{% endblock %}
